## 📋 NÁVRHY NA ZLEPŠENIE PROJECT_CONTROL

### 🎯 1. MENU.PY - 100% ROBUSTNÉ A USER-FRIENDLY

#### 1.1 Kritické bezpečnostné a error handling

  # ✅ PRIDAŤ
  - Validácia vstupov pred každým action
  - Try-catch bloky okolo všetkých služobných volaní
  - Recovery pre corrupted state files
  - Graceful degradation keď niektoré časti nefungujú
  - Cancel button pre dlhé operácie (Ctrl+C handling)
  - Auto-backup pred kritickými operáciami

  # ❌ ODSTRÁNIŤ
  - Silné závislosti na "magic" strings bez konštánt
  - Priame volaní bez error handlingu

#### 1.2 User Experience vylepšenia

  # ✅ PRIDAŤ
  - Progress bary pre dlhé operácie (scan, graph build)
  - Color highlighting (farby pre rôzne stavy)
  - Kontextová nápoveda (H/F1 klávesa)
  - Quick actions (scan → ghost → graph v jednom kroku)
  - Favorites/Bookmarks pre často traceované ciele
  - Auto-refresh statusov v header
  - Search/filter v menu (pre výber súborov pri trace)
  - Confirm pred DESTRUCTIVE akciami (rebuild, clear cache)
  - History/undo pre posledné akcie
  - Notifications systém (warnings pre orphans, cycles)
  - Keyboard shortcuts (1-5, q=quit, s=settings, h=help)

  # ❌ ODSTRÁNIŤ
  - Zbytočné "Press Enter to return" po každej akcii
  - Opakujúce sa code v settings menu (_change_* funkcie)

#### 1.3 Nové menu sekcie

  # ✅ PRIDAŤ
  6) Quick     — quick actions (scan+ghost+graph, health check)
  7) Reports   — all reports in one place (ghost, graph, checklist)
  8) Tools     — utilities (clear cache, export config, validate project)
  9) Help      — contextual help, commands reference, tutorials

--------

### 🏗️ 2. ARCHITEKTÚRA - ROBUSTNEJŠIA

#### 2.1 Centralizovaný Error Handling

  # NOVÝ: core/error_handler.py
  class ProjectControlError(Exception):
      """Base exception for all PROJECT_CONTROL errors."""

  class ValidationError(ProjectControlError):
      """Validation failed."""

  class OperationError(ProjectControlError):
      """Operation failed."""

  def handle_error(error: Exception, context: str = "") -> int:
      """Centralized error handling with user-friendly messages."""
      # Log, display, return appropriate exit code

#### 2.2 Service Layer Protocol

  # NOVÝ: services/base.py
  from typing import Protocol, Any

  class Service(Protocol):
      def execute(self, project_root: Path, **kwargs) -> ServiceResult:
          """Execute service and return structured result."""
          ...

  @dataclass
  class ServiceResult:
      success: bool
      message: str
      data: Any = None
      exit_code: int = 0

#### 2.3 State Consolidation

  # ZLÚČIŤ:
  - .project-control/config.json (UI state)
  - .project-control/status.yaml (tags)

  DO JEDNÉHO:
  - .project-control/state.json (všetok stav)
    - ui: {mode, profile, ...}
    - tags: {...}
    - favorites: [trace targets, ...]
    - history: [last actions, ...]
    - metadata: {last_scan, last_build, ...}

#### 2.4 Transactional Operations

  # NOVÝ: core/transaction.py
  class Transaction:
      """Atomic operations with rollback support."""
      def __init__(self, project_root: Path):
          self.project_root = project_root
          self.backup_dir = project_root / ".project-control" / "backups"

      def begin(self):
          """Create backup before operation."""

      def commit(self):
          """Remove backup after success."""

      def rollback(self):
          """Restore from backup on failure."""

#### 2.5 Health Check System

  # NOVÝ: core/health.py
  def health_check(project_root: Path) -> HealthReport:
      """Check project health and return actionable issues."""
      return HealthReport(
          snapshot_exists=...,
          snapshot_valid=...,
          graph_exists=...,
          graph_fresh=...,
          ripgrep_available=...,
          ollama_available=...,
          issues=[...],
          suggestions=[...],
      )

--------

### 🚀 3. NOVÉ FUNKCIE - USER-FRIENDLY

#### 3.1 Quick Actions

  # NOVÉ v menu:
  Quick Actions:
    1) Full Analysis      — scan → ghost → graph → report
    2) Health Check       — validate everything
    3) Find Orphans       — quick orphan scan
    4) Find Cycles        — quick cycle detection
    5) Dependency Audit   — analyze dependency graph

#### 3.2 Favorites & History

  # NOVÁ funkcionalita:
  - Ukladať často traceované ciele
  - Quick access k recent files
  - Auto-suggest pri trace inpute
  - History pre posledných 10 akcií

#### 3.3 Smart Notifications

  # NOVÉ:
  - "You have 31 orphans. Run 'Analyze → Ghost' for details."
  - "Graph is 3 days old. Rebuild recommended."
  - "New files detected. Run scan to update."
  - "Ripgrep not found. Some features limited."

#### 3.4 Project Templates/Presets

  # NOVÉ:
  - "React Frontend" preset (JS/TS, node_modules ignored, ...)
  - "Python Backend" preset (Python, __pycache__ ignored, ...)
  - "Full Stack" preset (Mixed, both languages)
  - "Custom" preset (user-defined)

#### 3.5 Export/Import Settings

  # NOVÉ:
  - Export .project-control/state.json ako shareable config
  - Import settings z iného projektu
  - Git-friendly diff-friendly state

#### 3.6 Interactive File Explorer

  # NOVÉ v menu:
  7) Browse    — interactive file tree with info
     - Show file metadata (size, last modified)
     - Show dependencies (who imports this)
     - Show dependents (what this imports)
     - Quick actions: trace, ghost check, open in editor

--------

### 🎨 4. VISUAL & UX VYLEPŠENIA

#### 4.1 Color Terminal Output

  # NOVÝ: utils/terminal.py
  class Colors:
      HEADER = "\033[95m"
      OKBLUE = "\033[94m"
      OKCYAN = "\033[96m"
      OKGREEN = "\033[92m"
      WARNING = "\033[93m"
      FAIL = "\033[91m"
      ENDC = "\033[0m"

  def print_success(msg: str): ...
  def print_warning(msg: str): ...
  def print_error(msg: str): ...
  def print_info(msg: str): ...

#### 4.2 Progress Indicators

  # NOVÝ: utils/progress.py
  def show_progress(current: int, total: int, message: str = ""):
      """Show progress bar for long operations."""
      # [████████░░] 80% Scanning files...

#### 4.3 Rich Tables

  # NOVÉ formátovanie výstupov:
  ┌─────────────┬─────────┬─────────┬──────────┐
  │ File        │ Orphan  │ Cycle   │ Actions  │
  ├─────────────┼─────────┼─────────┼──────────┤
  │ utils.py    │ No      │ Yes     │ [T]race  │
  │ helpers.js  │ Yes     │ No      │ [D]elete │
  └─────────────┴─────────┴─────────┴──────────┘

--------

### 🛡️ 5. ROBUSTNOSŤ & VALIDÁCIA

#### 5.1 Pre-flight Checks

  # PRED KAŽDOU AKCIOU:
  - Snapshot exists? (nie, show: "Run scan first")
  - Graph exists? (nie, show: "Build graph first")
  - Ripgrep available? (nie, warn, degrade gracefully)
  - Ollama running? (nie, warn, skip embedding)
  - Disk space ok? (nie, show error)

#### 5.2 Auto-recovery

  # PRI ERROROCH:
  - Corrupted snapshot? → Show error, suggest rescan
  - Corrupted graph? → Show error, suggest rebuild
  - Corrupted state? → Restore from backup or reset

#### 5.3 Validation Layer

  # NOVÝ: core/validator.py
  def validate_snapshot(snapshot: Dict) -> ValidationResult:
      """Validate snapshot structure and integrity."""
      ...

  def validate_graph(graph: Dict) -> ValidationResult:
      """Validate graph structure and consistency."""
      ...

--------

### 📊 6. DASHBOARD & OVERVIEW

#### 6.1 Project Dashboard

  # NOVÉ v menu:
  D) Dashboard — project overview
  ┌─────────────────────────────────────┐
  │ PROJECT: my-app                     │
  ├─────────────────────────────────────┤
  │ Snapshot:  OK (247 files)           │
  │ Graph:     OK (247 nodes, 312 edges)│
  │ Orphans:   31 ⚠️                    │
  │ Cycles:    5 ⚠️                     │
  │ Last scan: 2 hours ago              │
  │ Last build: 1 hour ago              │
  ├─────────────────────────────────────┤
  │ Warnings:                           │
  │ • 31 orphan files found             │
  │ • 5 circular dependencies           │
  │ • Ripgrep not installed             │
  ├─────────────────────────────────────┤
  │ Quick Actions:                      │
  │ [1] View orphans  [2] View cycles  │
  │ [3] Rescan        [4] Rebuild       │
  └─────────────────────────────────────┘

--------

### 🔧 7. TOOLS MENU

  # NOVÉ:
  8) Tools
     1) Clear Cache          — remove .project-control/out/
     2) Validate Project     — check everything
     3) Export Config        — export state.json
     4) Import Config        — import state.json
     5) Reset Settings       — reset to defaults
     6) View Logs            — show recent logs
     7) Diagnostics          — show system info

--------

### 🎓 8. HELP & TUTORIALS

  # NOVÉ:
  9) Help
     1) Quick Start          — 30-second tutorial
     2) Command Reference    — all commands
     3) Ghost Analysis       — what it finds
     4) Dependency Graph     — how it works
     5) Troubleshooting      — common issues
     6) Keyboard Shortcuts   — all shortcuts

--------

### 📝 9. SPECIFIKNE MENU.PY VYLEPŠENIA

#### 9.1 Refactor Settings Menu

  # ✅ ZMENIŤ:
  def _settings_menu(project_root: Path, state: AppState) -> AppState:
      # ZLÚČIŤ všetky _change_* do jednej generickej funkcie
      # Použiť MenuBuilder pattern
      # Pridať "Back to main" shortcut (b)

#### 9.2 Keyboard Handler

  # NOVÝ:
  class KeyboardHandler:
      """Handle keyboard shortcuts and special keys."""
      SHORTCUTS = {
          'q': 'quit',
          'b': 'back',
          'h': 'help',
          'r': 'refresh',
          's': 'settings',
          # ...
      }

#### 9.3 Menu Builder Pattern

  # NOVÝ:
  class MenuBuilder:
      """Build menus dynamically."""
      def add_item(self, key: str, label: str, action: Callable):
          ...
      def add_separator(self):
          ...
      def render(self) -> str:
          ...

--------

### 🚨 10. CRITICAL: ROLLBACK & BACKUP

  # NOVÝ: core/backup.py
  class BackupManager:
      """Manage .project-control backups."""
      def create_backup(self, name: str = "") -> Path:
          """Create timestamped backup."""

      def list_backups(self) -> List[Backup]:
          """List all backups."""

      def restore_backup(self, backup: Backup):
          """Restore from backup."""

      def cleanup_old_backups(self, keep: int = 5):
          """Remove old backups, keep last N."""

--------

## 🎯 PRIORITNÉ ZLEPŠENIA (TOP 5)

--------

## 🎯 PRIORITNÉ ZLEPŠENIA (TOP 5)

### 1. Error Handling & Validation (Kritické)

• Centralizovaný error handling
• Pre-flight checks pred každou akciou
• Graceful degradation

### 2. Progress Indicators (UX kritické)

• Progress bary pre scan, graph build
• Vizuálny feedback pre dlhé operácie

### 3. Dashboard (User-friendly)

• Projekt overview na jednej obrazovke
• Warnings, notifications, quick actions

### 4. Backup & Rollback (Robustnosť)

• Auto-backup pred kritickými operáciami
• Recovery pre corrupted state

### 5. Quick Actions (User-friendly)

• "Full Analysis" v jednom kroku
• Health check
• Common actions jednou klávesou

--------

Chceš, aby som začal implementova