"""Utility helpers for controlled debug printing."""

from __future__ import annotations


def debug_print(enabled: bool, *args, **kwargs) -> None:
    """Print only when debugging is enabled."""
    if enabled:
        print(*args, **kwargs)
